# Downloads the contents of a notion page, generates the proper directory structure for the instructions, and commits them to GitHub
import json
import os


def lambda_handler(event, context):
    page_data = json.loads(event)


def download_page():
    return

